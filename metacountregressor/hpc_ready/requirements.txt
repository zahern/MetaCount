# MetaCount HPC requirements
# Install with:  pip install -r requirements.txt
#
# Tested on Python 3.10 / 3.11 / 3.12

# Core JAX stack — CPU-only (replace jax[cpu] with jax[cuda12] for GPU)
jax[cpu]>=0.4.20
jaxopt>=0.8.1

# Scientific stack
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3

# Optimisation / metaheuristics (bundled in repo, listed here for pip installs)
# No extra package needed — uses the repo's own metaheuristics.py

# Optional: faster Halton draws
# qmcpy>=1.3
